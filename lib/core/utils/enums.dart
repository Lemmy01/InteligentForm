enum AccountType {
  individual,
  company,
  publicInstitution,
}

enum FieldType {
  text,
  number,
  decimal,
  date,
  singleChoice,
  multipleChoice,
}

enum ScanDocumentType {
  none,
  identityCard,
  birthCertificate,
  passport,
  vehicleIdentityCard,
  anyDocument,
}
